import React from "react";
import { Layout } from "../components/UI/Layout";

const NotFoundPage = () => {
  return <Layout title="404, not found">404, page not found</Layout>;
};

export default NotFoundPage;
